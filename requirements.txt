# To-Do Pro - Dependências Python
# =================================

# Framework Web
Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3

# Banco de Dados
SQLAlchemy==2.0.23

# Segurança
Werkzeug==3.0.1

# Bootstrap (via CDN no HTML)
# Font Awesome (via CDN no HTML)
# Google Fonts (via CDN no HTML)

# Python-dotenv (opcional, para variáveis de ambiente)
python-dotenv==1.0.0

# Ranqueamento de cores para Priority
# (já incluso no Python padrão)
